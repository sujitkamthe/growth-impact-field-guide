{
  "name": "sahaj-competency-framework-site",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "npx concurrently --kill-others \"npx nodemon --watch content -e md --exec 'node build.js'\" \"npx live-server --watch=manifest.json\""
  },
  "devDependencies": {
    "playwright": "^1.58.0"
  },
  "dependencies": {
    "marked": "^18.0.0"
  }
}
